package minio

// PublicOrgID is used for system templates (IsSystem=true) that are accessible to all orgs.
// Templates with IsSystem=true are stored under this "virtual" org for easy identification.
const PublicOrgID = "mapexos_public"
