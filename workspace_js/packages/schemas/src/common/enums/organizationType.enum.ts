/**
 * Enum to represent different types of organizations in the hierarchy
 */
export enum OrganizationTypeEnum {
	VENDOR = 'vendor',
	CUSTOMER = 'customer',
	SITE = 'site',
	BUILDING = 'building',
	FLOOR = 'floor',
	ZONE = 'zone',
}
