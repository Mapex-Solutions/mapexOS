/**
 * Scripts module utilities.
 *
 * Empty barrel for now — the legacy LEAF subject parser used to live
 * here but the broker plugin now carries every routing field
 * (orgId, assetUUID) inside the JSON payload, so no subject parsing
 * is needed. mapexValidatorCode and script-compression moved to
 * @shared/utils.
 */
export {};
