/** HTTP port for the marketplace mock server */
export const MARKETPLACE_PORT = 3099;

/** Relative path (from src/) to the public directory that hosts plugin manifests */
export const PUBLIC_DIR_RELATIVE = '../public';
