/**
 * Default maximum height for the scroll area (in pixels)
 * Applied when the list has more than 10 items
 */
export const DEFAULT_MAX_HEIGHT = 300;

/**
 * Number of fields threshold to enable scrolling
 * If fields.length > this value, scroll area with maxHeight is applied
 */
export const SCROLL_THRESHOLD = 10;
