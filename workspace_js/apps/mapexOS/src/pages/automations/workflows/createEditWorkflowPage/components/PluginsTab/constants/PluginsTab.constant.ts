/** Base URL for the plugin CDN (brand icons, manifests) */
export const PLUGIN_CDN_BASE_URL = process.env.PLUGIN_CDN_BASE_URL as string;

/** Max items per page in the marketplace grid */
export const MARKETPLACE_PAGE_SIZE = 12;
